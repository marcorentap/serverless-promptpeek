# Execution
1) Install [uv](https://docs.astral.sh/uv/)
2) `uv sync` to install dependencies and create venv
3) Install [PyTorch](https://pytorch.org/get-started/locally/) with `uv pip install...`
4) Run `cat input.txt | uv run attack.py`
